mkdir -p out
find testdata -name '*.json' -print0 |
while IFS= read -r -d '' f; do
  base="$(basename "${f%.json}")"
  go run ./cmd/cdt_example/ --load "$f" \
    --output "out/${base}.png" \
    --width 2048 --height 2048
done
